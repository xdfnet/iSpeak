#!/bin/bash
# speak — 发送文本到 iSpeak TTS 服务
SOCK=/tmp/iagent.tts.sock
[[ -z "$1" && -t 0 ]] && { echo "usage: echo 'text' | speak   or   speak 'text'" >&2; exit 1; }
text="${*:-$(cat)}"
[[ -z "$text" ]] && exit 0
echo "$text" | nc -U -w5 "$SOCK" 2>/dev/null
